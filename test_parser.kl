# Test file for parser validation
# Simpler version to verify syntax checking

int main() {
    int x = 42;
    int y = 100;
    float pi = 3.14;
    
    if (x > 10) {
        y++;
    } else {
        y--;
    }
    
    for (int i = 0; i < 10; i++) {
        x = x + 1;
    }
    
    while (y > 0) {
        y--;
    }
    
    return 0;
}

int add(int a, int b) {
    int result = a + b;
    return result;
}

struct Point {
    int x;
    int y;
};
