# PARSER TEST RESULTS - COMPLETE OUTPUT

## TEST 1: test_parser.kl - Basic Syntax Validation

**Test Purpose:** Verify parser accepts valid C-like syntax including functions, control flow, and structs

**Input Code:**
```c
# Test file for parser validation
# Simpler version to verify syntax checking

int main() {
    int x = 42;
    int y = 100;
    float pi = 3.14;
    
    if (x > 10) {
        y++;
    } else {
        y--;
    }
    
    for (int i = 0; i < 10; i++) {
        x = x + 1;
    }
    
    while (y > 0) {
        y--;
    }
    
    return 0;
}

int add(int a, int b) {
    int result = a + b;
    return result;
}

struct Point {
    int x;
    int y;
};
```

**Parser Output:**
```
=== KernaLex Parser ===
Input file: parser_test/test_parser.kl
====================================


====================================
✓ Parsing completed successfully!
✓ No syntax errors detected.
====================================
```
---

## TEST 2: demo_test.kl - Recursive Function

**Test Purpose:** Verify parser handles recursive function calls and nested expressions

**Input Code:**
```c
# Demo: Valid KernaLex program
int fibonacci(int n) {
    if (n <= 1) {
        return n;
    }
    return fibonacci(n - 1) + fibonacci(n - 2);
}

int main() {
    int result = fibonacci(10);
    return 0;
}
```

**Parser Output:**
```
=== KernaLex Parser ===
Input file: parser_test/demo_test.kl
====================================


====================================
✓ Parsing completed successfully!
✓ No syntax errors detected.
====================================
```

---

## TEST 3: test_edge_parser.kl - Advanced Constructs

**Test Purpose:** Verify parser handles arrays, pointers, structs, switch, repeat-until

**Input Code:**
```c
# Edge case parser tests (valid syntax)

struct Point {
    int x;
    int y;
};

int sum(int a, int b) {
    return a + b;
}

int main() {
    int i = 0;
    int arr[3];
    int* p = &arr[0];
    int value = *p;

    struct Point pt;
    pt.x = 1;
    pt.y = 2;

    switch (i) {
        case 0:
            i = 1;
            break;
        default:
            i = 2;
    }

    for (int j = 0; j < 3; j++) {
        arr[j] = j;
    }

    while (i < 10) {
        i++;
    }

    repeat {
        i--;
    } until (i == 0);

    if (i == 0) {
        i = 1;
    } else {
        i = 2;
    }

    return 0;
}
```

**Parser Output:**
```
=== KernaLex Parser ===
Input file: parser_test/test_edge_parser.kl
====================================


====================================
✓ Parsing completed successfully!
✓ No syntax errors detected.
====================================
```

---

## TEST 4: test_edge_parser_errors.kl - Syntax Error Detection

**Test Purpose:** Verify parser detects missing semicolons and mismatched braces

**Input Code:**
```c
# Edge case parser tests (invalid syntax)

int main() {
    int x = 1       # ERROR: Missing semicolon
    if (x > 0) {
        x++;
    else {          # ERROR: Missing closing brace before else
        x--;
    }

    for (int i = 0; i < 5; i++) {
        x = x + 1   # ERROR: Missing semicolon
    }

    return 0;
}
```

**Parser Output:**
```
[SYNTAX ERROR: Line 5: syntax error near 'if']
=== KernaLex Parser ===
Input file: parser_test/test_edge_parser_errors.kl
====================================


====================================
✗ Parsing failed with 1 syntax error(s).
====================================
```

---

## TEST 5: test_syntax_errors.kl - Multiple Syntax Errors

**Test Purpose:** Verify parser detects various syntax errors and reports them

**Input Code:**
```c
# Test file with intentional syntax errors

int main() {
    int x = 42;
    
    # Error 1: Missing semicolon
    int y = 100     # ERROR: Missing semicolon
    
    # Error 2: Mismatched braces
    if (x > 10) {
        y++;
    # ERROR: Missing closing brace
    
    # Error 3: Invalid expression
    x = + + 5;      # ERROR: Invalid syntax
    
    return 0;
}

# Error 4: Function with missing closing brace
int test() {
    int a = 5;
# ERROR: Missing closing brace
```

**Parser Output:**
```
[SYNTAX ERROR: Line 10: syntax error near 'if']
=== KernaLex Parser ===
Input file: parser_test/test_syntax_errors.kl
====================================


====================================
✗ Parsing failed with 1 syntax error(s).
====================================
```


